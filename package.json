{
  "name": "omnibox",
  "version": "1.0.0",
  "description": "Fast url parsing with a tiny footprint",
  "main": "omnibox.js",
  "scripts": {
    "build": "jshint . && browserify -s omnibox -do dist/omnibox.js omnibox.js && uglifyjs -m -c -o dist/omnibox.min.js dist/omnibox.js",
    "deployment": "git add dist && npm version ${BUMP:-\"patch\"} --no-git-tag-version && git add package.json && git commit -am \"Autogenerated pre-deployment commit\" && bower version ${BUMP:-\"patch\"} && git reset HEAD~2 && git add . && git commit -am \"Release $(cat package.json | jq -r .version)\" && git push --tags && npm publish && git push",
    "deploy": "npm run build && npm run deployment && npm run sync"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bevacqua/omnibox.git"
  },
  "author": "Nicolas Bevacqua <nicolasbevacqua@gmail.com> (http://bevacqua.io/)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/bevacqua/omnibox/issues"
  },
  "homepage": "https://github.com/bevacqua/omnibox",
  "dependencies": {
  },
  "devDependencies": {
    "browserify": "8.1.0",
    "jshint": "2.5.11",
    "uglify-js": "2.4.16"
  }
}
